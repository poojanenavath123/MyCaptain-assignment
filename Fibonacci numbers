def fibonacci(n):
    fib_sequence = [0, 1]

    while len(fib_sequence) < n:
        fib_sequence.append(fib_sequence[-1] + fib_sequence[-2])

    return fib_sequence[:n]

# Example usage
n = 14  # This will generate the first 14 Fibonacci numbers
fib_numbers = fibonacci(n)
print(fib_numbers)

